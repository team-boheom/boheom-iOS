import UIKit

extension UILabel {
    func boheomLabel(text: String? = nil, font: UIFont?, textColor: UIColor = .black) {
        self.text = text
        self.font = font
        self.textColor = textColor
    }
}
